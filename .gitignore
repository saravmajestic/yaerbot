# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node
node_modules/

# Dataset captures / model artifacts (kept out of git)
captures/
*.eim

# Generated demo outputs (regenerate with examples/plain_field_demo.py)
examples/report.svg
examples/runlog.json

# OS / editor
.DS_Store
*.swp

# Secrets / local scratch (never commit)
*.local
secrets.*
commands.txt
examples/full_report.svg
farmos/planner/data/weather_cache.json

# Runtime logs written by the console on the board
console/battery.csv

# Persistent operator log written by the console at run time (see log() in main.py).
# It exists so a run survives an app restart; it is machine output, not source.
console/console.log
console/console.log.1

# Demo video: 561 MB, far past GitHub's 100 MB per-file push limit.
# Keep footage out of the repo — upload it to the submission form or a host.
assets/*.mp4
assets/*.mov
